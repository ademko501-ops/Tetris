# Проект: Tetris Vault-Tec Edition

## Кто я
Сергей, учусь вайб-кодингу. Не разработчик. Объясняй термины
простыми словами, расшифровывай аббревиатуры.

## Роли в проекте
- **Сергей** — архитектор, заказчик, идейный вдохновитель, оценщик.
  НЕ пишет код руками.
- **Claude Chat** — обучение, планы, ревью, генерация PDF-артефактов
  курса (этапы R-N и учебные модули).
- **Claude Code** — пишет код, правит файлы, делает Git-операции
  (add/commit/push), обновляет .md-документы.
- **Cowork** — рутина с файлами вне кода.

## Что делаем
Игра Тетрис в браузере. Стек: HTML + CSS + JavaScript.
Поле 12×24, 7 классических фигур, управление клавиатурой.
Потом упакуем в десктоп через Electron.

## Стиль
Ретро-футуризм Fallout / Pip-Boy:
- Зелёное свечение CRT (#15ff00)
- Янтарный акцент (#ffb000)
- Тёмный фон, скан-линии, моноширинный шрифт

## Правила работы со мной
- Объясняй ПЕРЕД тем как писать код, что и зачем
- Не используй сложные библиотеки без согласования
- После каждого изменения — кратко скажи что поменял
- В коде — комментарии простым языком

## Протокол закрытия этапа
Когда Сергей говорит **«закрой этап R-N»** или **«закрой R-N hash=XXX»**:

а) Проверь, появился ли новый файл `docs/Stage_R-N.pdf`.
б) Обнови `PROGRESS.md` — допиши новый раздел в конец файла
   (старые разделы не переписывать).
в) Обнови `GLOSSARY.md`, если в этапе были новые термины.
г) Сделай атомарный коммит документации:
   - `git add .`
   - `git commit -m "docs: закрыт R-N + артефакт"`
   - `git push`
д) Дай короткий рапорт: что обновлено, хеш нового коммита,
   ссылку на коммит на GitHub.

## Правила атомарных коммитов
- **Код этапа R-N** — отдельный коммит с подписью `"R-N: что сделано"`.
- **Документация** (PROGRESS, GLOSSARY, CLAUDE.md и др.) —
  отдельный коммит с подписью `"docs: ..."`.
- **PDF-артефакт** из `docs/` — может идти вместе с docs-коммитом
  (одним коммитом с обновлением PROGRESS).
- **НЕ смешивать** в одном коммите код и доки.

## Структура файлов
- src/index.html — главная страница
- src/style.css  — оформление
- src/game.js    — логика игры
- PROGRESS.md    — обновляй после каждого этапа

## Папка docs/
Сюда складываются все PDF-артефакты курса.

**Именование:**
- `Stage_R-N.pdf` — этапы кода (например, `Stage_R-1.pdf`, `Stage_R-2.pdf`).
- `Module_N.pdf` или `Module_N-M.pdf` — учебные модули
  (например, `Tetris_Course_Module1.pdf`, `Tetris_Course_Module_2-1.pdf`).

Сергей вручную скачивает PDF из Claude Chat и кладёт в эту папку.
Claude Code эти файлы не генерирует, только коммитит и пушит вместе
с docs-коммитом по протоколу закрытия этапа.

## Текущий статус
Этап 1.3 завершён. Папка создана, документация заведена.
Следующий шаг — Модуль 2 (Git и GitHub).
